{
  "name": "miyansol-inventory",
  "packageManager": "pnpm@9.15.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n API,WEB -c yellow,cyan \"pnpm --filter stockmate-api dev\" \"pnpm --filter stockmate-web dev\"",
    "build": "pnpm --filter stockmate-api build && pnpm --filter stockmate-web build",
    "start": "pnpm --filter stockmate-api start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
